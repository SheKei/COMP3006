<!DOCTYPE html>
<html lang="en">
<head>
    <title>View Book</title>
    <base href="/">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="./css/view_book.css"/>
</head>
<body>
<header><%- include("User_Navbar"); %></header>
    <div id="viewBookPage">
        <div id="bookContainer">
            <input type="number" class="basketNum hidden" value="<%= basketNum %>" readonly>
            <div class="row">
                <div class="col-6"><br><br><br>
                    <div><img src="/images/<%= book.getImage() %>" ></div><br>
                    <div class="text-center">
                        <button type="submit" class="btn btn-dark">
                            <a href="/View_All_Books">Go Back</a>
                        </button>
                    </div>
                </div>
                <div class="col-6">
                    <div><h2 class="text-center"> <%= book.getBookName() %></h2></div>
                    <div><h3 class="text-center">By <%= book.getAuthorForename() %> <%= book.getAuthorSurname() %></h3></div><br>
                    <div><h3 class="text-center">Genres: <%= theGenres %></h3></div><br><br>
                    <div><section><p id="synopsis"><strong>Synopsis: </strong><%= book.getSynopsis() %></p></section></div><br>
                    <div><h3>£<%= book.getSellingPrice() %></h3></div>
                    <% if(book.getStockAmount() > 0){ %>
                    <form method="post" action="addToBasket">
                    <div class="form-group row">
                        <input type="text" name="bookID" id="bookID" value="<%= book.getId() %>" readonly>
                        <div class="col-3">
                            <label for="orderAmount">Quantity:</label>
                        </div>
                        <div class="col-2">
                            <input type="number" value="1" min="1" class="form-control" name="orderAmount" id="orderAmount" placeholder="1" required>
                        </div>
                        <div class="col-4">
                            <button type="submit" class="btn btn-dark">Add to Basket</button>
                        </div>
                    </div>
                    <% }else{ %>
                            <h2 style="letter-spacing: 3px;" class="text-center">OUT OF STOCK</h2>
                        <% } %>
                    </form>

                </div>
            </div>
        </div>
    </div>
</body>
</html>
