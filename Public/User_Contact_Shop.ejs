<!DOCTYPE html>
<html lang="en">
<head>
    <title>Contact Shop</title>
    <base href="/">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://kit.fontawesome.com/20c7401107.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="./css/user_contact_shop.css"/>
    <script src="https://cdn.socket.io/socket.io-3.0.1.min.js"></script>
    <script type="text/javascript" src="/jquery/user_contact_shop.js"></script>
</head>
<body>
<header><%- include("User_Navbar"); %></header>
    <div id="contactSupportPage">
        <input type="number" class="basketNum hidden" value="<%= basketNum %>" readonly>
        <div id="contactSupportContainer">
            <h2>Contact Shop</h2><br>
            <div class="row">
                <div class="col-4">
                    <div class="text-center">
                        <span><i id="icon" class="fas fa-circle fa-2x inactive"></i><span class="status">Admin Status: <span id="status">OFFLINE</span></span></span>
                    </div>
                    <br>
                    <h5 id="userID" class="hidden"><%= userID %></h5>
                    <textarea class="form-control" rows="6" id="msg" name="msg" required></textarea><br>
                    <div class="text-center"><button type="button" id="sendMsgBtn" class="btn btn-dark">Send</button></div>
                </div>
                <div class="col-8">
                    <div id="msgBoard">
                        <p id="messages"></p>
                        <% if(msg.length > 0){%>
                            <% for(i=0; i<msg.length; i++) { %>
                                <% if(msg[i].getSender() !== "admin"){%>
                                    <div class="customerDiv">
                                        <p class="customer"><span><%= msg[i].getMessage() %></span></p>
                                        <p class="customer font-weight-bold"><%= msg[i].getTimestamp() %></p>
                                    </div><br><br>
                                <%}else{%>
                                    <div class="adminDiv">
                                        <p class="admin">Admin: <%= msg[i].getMessage() %></p>
                                        <p class="admin font-weight-bold"><%= msg[i].getTimestamp() %></p>
                                    </div><br><br><br><br><br><br>
                                <%}%>
                            <%}%>
                        <%}%>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
<script>

</script>


